# Reglas del Proyecto
- Cada vez que se realice un cambio en el código del proyecto, se debe volver a levantar o reiniciar el contenedor de Docker ejecutando `docker compose up -d --build` para asegurar la correcta visualización de los cambios en el entorno local.
